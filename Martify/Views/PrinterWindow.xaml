<Window x:Class="Martify.Views.PrinterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="In Hóa Đơn" Height="750" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Storyboard x:Key="PrintAnimation">
            <DoubleAnimation Storyboard.TargetName="ReceiptPaperTranslate"
                             Storyboard.TargetProperty="Y"
                             From="-650" To="15" Duration="0:0:3.5">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="1"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ActionButtons" 
                                           Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:3.5" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>

        </Storyboard>
    </Window.Resources>

    <Grid>
        <Border Background="#99000000" CornerRadius="20" MouseLeftButtonDown="Border_MouseLeftButtonDown"/>

        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Panel.ZIndex="2" 
                    Background="#333333" CornerRadius="10" 
                    Height="60" Width="400" Margin="0,0,0,-18">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="5" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Border Background="#111111" Height="8" Margin="20,0" VerticalAlignment="Bottom" CornerRadius="4"/>
                    <Ellipse Width="8" Height="8" Fill="#00FF00" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                        <Ellipse.Effect>
                            <DropShadowEffect Color="#00FF00" BlurRadius="5" ShadowDepth="0"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <TextBlock Text="Martify Printer" Foreground="#666" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Roboto Medium" FontSize="10"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Width="380" Height="550" ClipToBounds="True">

                <ScrollViewer x:Name="MainScrollViewer" 
                              VerticalScrollBarVisibility="Hidden" 
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="0,20,0,0">

                    <Border x:Name="ReceiptTicket" Background="White" Width="340" VerticalAlignment="Top" 
                            Padding="20" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Border.Effect>

                        <Border.RenderTransform>
                            <TranslateTransform x:Name="ReceiptPaperTranslate" Y="-650"/>
                        </Border.RenderTransform>

                        <StackPanel DataContext="{Binding CurrentInvoice}">

                            <TextBlock Text="Martify" 
                                       FontSize="60" 
                                       Foreground="{DynamicResource PrimaryText}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" 
                                       FontFamily="{StaticResource FleurDeleah}"
                                       Margin="0,-10,0,0"/>

                            <TextBlock Text="Hóa đơn thanh toán" 
                                       FontSize="22" 
                                       Foreground="{DynamicResource PrimaryText}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" 
                                       FontFamily="{StaticResource Charm}"/>

                            <TextBlock Text="{Binding InvoiceID}" 
                                       FontSize="12" 
                                       Foreground="{DynamicResource SecondaryText}" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,2,0,0"/>

                            <Separator Margin="0,10" Background="{DynamicResource MaterialDesignDivider}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Ngày tạo:" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                                    <TextBlock Text="{Binding CreatedDate, StringFormat='dd/MM/yyyy HH:mm'}" FontSize="13" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock Text="Nhân viên:" FontSize="11" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding Employee.FullName}" FontSize="13" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>

                            <Separator Margin="0,10,0,5" Background="{DynamicResource MaterialDesignDivider}"/>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="1.75*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Sản phẩm" FontSize="12"/>
                                    <TextBlock Grid.Column="1" Text="Đơn giá" HorizontalAlignment="Right" FontSize="12"/>
                                    <TextBlock Grid.Column="2" Text="SL" HorizontalAlignment="Center" FontSize="12"/>
                                    <TextBlock Grid.Column="3" Text="Thành tiền" HorizontalAlignment="Right" FontSize="12"/>
                                </Grid>

                                <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="Black" Opacity="0.1"/>

                                <ItemsControl Grid.Row="1" ItemsSource="{Binding InvoiceDetails}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Product.ProductName}" 
                                                           FontWeight="SemiBold" FontSize="12"
                                                           Foreground="{DynamicResource PrimaryText}" 
                                                           TextWrapping="Wrap"/>

                                                <Grid Grid.Row="1" Margin="0,2,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="2*"/>
                                                        <ColumnDefinition Width="1.5*"/>
                                                        <ColumnDefinition Width="0.8*"/>
                                                        <ColumnDefinition Width="1.75*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Text="{Binding Product.ProductID}" 
                                                               FontSize="10" Foreground="{DynamicResource SecondaryText}" 
                                                               TextTrimming="CharacterEllipsis"/>

                                                    <TextBlock Grid.Column="1" Text="{Binding SalePrice, StringFormat={}{0:N0}}" 
                                                               HorizontalAlignment="Right" FontSize="12" Foreground="{DynamicResource SecondaryText}"/>

                                                    <TextBlock Grid.Column="2" Text="{Binding Quantity}" 
                                                               HorizontalAlignment="Center" FontSize="12" Foreground="{DynamicResource SecondaryText}"/>

                                                    <TextBlock Grid.Column="3" Text="{Binding Total, StringFormat={}{0:N0}}" 
                                                               HorizontalAlignment="Right" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>
                                                </Grid>

                                                <Border Grid.Row="1" VerticalAlignment="Bottom" BorderBrush="#EEE" BorderThickness="0,0,0,1" Margin="0,5,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>

                            <Separator Margin="0,10,0,10" Background="Black"/>

                            <Grid Margin="0,0,0,15">
                                <TextBlock Text="Tổng tiền:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0} VND}" 
                                           FontSize="18" FontWeight="Bold" Foreground="#4CAF50" 
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <StackPanel Margin="0,10,0,0">
                                <TextBlock Text="Cảm ơn quý khách!" HorizontalAlignment="Center" FontStyle="Italic" Foreground="{DynamicResource SecondaryText}"/>
                                <materialDesign:PackIcon Kind="Barcode" Width="180" Height="50" HorizontalAlignment="Center" Margin="0,5"/>
                                <TextBlock Text="{Binding InvoiceID}" HorizontalAlignment="Center" FontSize="10" Foreground="{DynamicResource SecondaryText}"/>
                            </StackPanel>

                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </Grid>

            <StackPanel x:Name="ActionButtons" Grid.Row="2" Orientation="Horizontal" 
                        HorizontalAlignment="Center" Margin="0,20,0,0" Visibility="Hidden">

                <Button Content="Lưu PDF" 
                        Command="{Binding SavePdfCommand}"
                        Background="#FF5722" BorderBrush="{x:Null}"
                        materialDesign:ButtonAssist.CornerRadius="15" Margin="0,0,10,0">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FilePdfBox" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Lưu PDF"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Đóng" Command="{Binding CloseCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        BorderBrush="White" Foreground="White"
                        materialDesign:ButtonAssist.CornerRadius="15"/>
            </StackPanel>

        </Grid>
    </Grid>
</Window>