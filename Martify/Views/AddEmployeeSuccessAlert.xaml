<UserControl x:Class="Martify.Views.AddEmployeeSuccessAlert"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="#7F000000" CornerRadius="25">
        <Border Background="{DynamicResource CardBackground}" 
                CornerRadius="15" 
                Width="400" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <materialDesign:PackIcon Grid.Row="0" Kind="CheckCircle" 
                                         Width="60" Height="60" 
                                         Foreground="#4CAF50" 
                                         HorizontalAlignment="Center" 
                                         Margin="0,0,0,15"/>

                <TextBlock Grid.Row="1" Text="Thêm thành công!" 
                           FontSize="22" FontWeight="Bold" 
                           Foreground="{DynamicResource PrimaryText}" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,15"/>

                <Border Grid.Row="2" Background="{DynamicResource ItemBackground}" 
                        CornerRadius="10" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Thông tin đăng nhập mặc định:" 
                                   Foreground="{DynamicResource SecondaryText}" 
                                   FontSize="12" Margin="0,0,0,10" FontStyle="Italic"/>

                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Mã NV:" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                            <TextBlock Text="{Binding NewEmployeeID}" HorizontalAlignment="Right" Foreground="#067FF8" FontWeight="Bold"/>
                        </Grid>

                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Tài khoản:" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                            <TextBlock Text="{Binding NewUsername}" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryText}"/>
                        </Grid>

                        <Grid>
                            <TextBlock Text="Mật khẩu:" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                            <TextBlock Text="{Binding NewPassword}" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryText}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Button Grid.Row="3" Content="HOÀN TẤT"
                        Command="{Binding CloseSuccessAlertCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="45" Width="150"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        Background="#4CAF50" BorderBrush="{x:Null}"
                        Foreground="White" FontSize="14" FontWeight="Bold"/>
            </Grid>
        </Border>
    </Border>
</UserControl>